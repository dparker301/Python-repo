# There are two ways to do that, and for the PCP examination, you'll just need to load one of them assertions,
# water assertions, assertions, or certain assumptions in our program that we think should always be

def calculate_inverse(number):
  assert (number != 0), 'Got 0 as number!'   #
  return 1/number

calculate_inverse(0)

# In general, assertions are used for debugging and testing of code.